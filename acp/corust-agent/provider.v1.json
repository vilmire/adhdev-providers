{
  "type": "corust-agent-acp",
  "name": "Corust Agent (ACP)",
  "category": "acp",
  "displayName": "Corust Agent",
  "icon": "🦀",
  "install": "Download from https://github.com/Corust-ai/corust-agent-release/releases",
  "spawn": {
    "command": "corust-agent-acp",
    "args": [],
    "shell": false
  },
  "auth": [
    {
      "type": "agent",
      "id": "corust",
      "name": "Corust Auth",
      "description": "Authenticate through Corust AI setup"
    }
  ],
  "settings": {
    "autoApprove": {
      "type": "boolean",
      "default": false,
      "public": true,
      "label": "Auto Approve",
      "description": "Automatically approve all tool calls without user confirmation"
    },
    "approvalAlert": {
      "type": "boolean",
      "default": true,
      "public": true,
      "label": "Approval Alerts",
      "description": "Show notification when agent requires approval"
    },
    "longGeneratingAlert": {
      "type": "boolean",
      "default": true,
      "public": true,
      "label": "Long Generation Alert",
      "description": "Alert when generation takes too long"
    },
    "longGeneratingThresholdSec": {
      "type": "number",
      "default": 180,
      "public": true,
      "label": "Long Generation Threshold (sec)",
      "min": 30,
      "max": 600
    }
  },
  "providerVersion": "1.0.1",
  "contractVersion": 2,
  "capabilities": {
    "input": {
      "multipart": false,
      "mediaTypes": [
        "text"
      ]
    },
    "output": {
      "richContent": false,
      "mediaTypes": [
        "text"
      ]
    },
    "controls": {
      "typedResults": false
    }
  },
  "tier": "declarative-only",
  "session": {
    "idlePattern": {
      "regex": "\"method\"\\s*:\\s*\"agent/(?:ready|done|finished|completed)\"",
      "flags": "i",
      "description": "Agent finished a turn and is waiting for the next prompt"
    },
    "generatingPattern": {
      "regex": "\"method\"\\s*:\\s*\"agent/(?:thinking|toolCall|progress|streaming)\"",
      "flags": "i",
      "description": "Agent is actively working — emitting a notification or tool call"
    },
    "promptStyle": "mcp",
    "errorPatterns": [
      {
        "regex": "\"method\"\\s*:\\s*\"agent/approvalRequired\"",
        "flags": "i",
        "verdict": "waiting_approval",
        "description": "Agent is requesting user approval before proceeding"
      }
    ],
    "$schema": "adhdev:acp/session-protocol@1"
  }
}
